Test program for drawing different shapes.

RandomFigureController is a controller class that draws a random shape.
Shape is an abstract class for shapes.
FigureParams is a class with shape parameters (may be deprecated when we add more classes)
Circle is a class that represents drawing a circle in different colors.